<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Tax Tribunals App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= stylesheet_link_tag "govuk/index", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

  <body class="govuk-template__body">
    <header class="govuk-header" role="banner" data-module="govuk-header">
      <div class="govuk-header__container govuk-width-container">
        <div class="govuk-header__logo">
          <a href="/" class="govuk-header__link govuk-header__link--homepage">
            Tax and Tribunals Demo
          </a>
        </div>
        <nav class="govuk-header__navigation">
          <% if user_signed_in? %>
            <p class="govuk-body">Welcome, <%= current_user.email %>!</p>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "govuk-link" %>
          <% else %>
            <%= link_to "Sign Up", new_user_registration_path, class: "govuk-link" %> |
            <%= link_to "Login", new_user_session_path, class: "govuk-link" %>
          <% end %>
          <%= link_to "Appeals", appeals_path, class: "govuk-link" %>
        </nav>
      </div>
    </header>
    <main class="govuk-main-wrapper">
      <%= yield %>
    </main>
  </body>

</html>
